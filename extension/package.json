{
  "name": "extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc -b --noEmit && vite build && vite build -c vite.content.config.ts",
    "pack": "npm run build && cd dist && rm -f ../topdown-extension.zip && zip -qr ../topdown-extension.zip . && echo 'topdown-extension.zip ready — upload this to the Chrome Web Store'",
    "lint": "eslint src test",
    "typecheck": "tsc -b --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "linkedom": "^0.18.13",
    "tsx": "^4.19.2",
    "typescript": "^5.5.4",
    "vite": "^6.4.3"
  }
}
